public Boolean duplicateContacts(Contact c1, Contact c2) {
    Boolean contDuplicates = false;
    if (c1.Phone != null && c1.Phone == c2.Phone ||
        c1.Email != null && c1.Email == c2.Email) {
            contDuplicates = true;
    }
    
    return contDuplicates;
}