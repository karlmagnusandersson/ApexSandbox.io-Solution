public List<Integer> evenNumbers(Integer n) {
    List<Integer> returnList = new List<Integer>();
    
    Integer currentEvenInt = 2;
    for (Integer i = 0; i < n; i++) {
        returnList.add(currentEvenInt);
        currentEvenInt += 2;   
    }
    return returnList;
}